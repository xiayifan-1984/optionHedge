CMAKE_MINIMUM_REQUIRED(VERSION 3.7)
PROJECT(systemConfig)

INSTALL(DIRECTORY systemConfig DESTINATION "include")

INSTALL(DIRECTORY api/ctp/${CTP_VERSION}/include/ DESTINATION "include/ctp")
INSTALL(DIRECTORY api/ctp/${CTP_VERSION}/lib/ DESTINATION "lib/api")
